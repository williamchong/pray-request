# OS
.DS_Store

# Dependencies (the repo root is non-code; a stray install shouldn't be tracked)
node_modules/

# Local Claude Code settings (personal overrides); shared .claude/ config is committable
.claude/settings.local.json

# Logs
*.log
